name: test-unit-dist

on:
  push:

jobs:
  test-unit-dist:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install premake5
      uses: ./.github/actions/install-premake5

    - name: make --version
      run: make --version

    - name: Generate make files (gcc)
      run: premake5 gmake --cc=gcc

    - name: Build dist
      run: make config=dist

    - name: Run tests
      run: ./bin/Dist/hill test
